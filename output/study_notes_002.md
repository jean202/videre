# lecture

- Source: local
- Generated: 2026-03-03 23:47

## Study Notes

### 1. [00:00 - 02:00]

![frame 1](output/frames/frame_001.jpg)

안녕하세요 매너 코드입니다 지난 시간에는 고객과 관리자가 어떤 일을 수행하는지를 유스케이스 다이어그램으로 정리했습니다 그리고 누락된 유스케이스들을 모두 보완하여 지금과 같은 형태로 구성했죠 그랬더니... 유스케이스 다이어그램이 왜 영어로 되어 있죠? 저는 읽지 말란 건가요? 이렇게 영어로 하면 개발할 때 편하거든요 저는 불편한데요 저런 그렇습니다 이 다이어그램은 분석 단계의 문서입니다 즉, 도메인 전문가나 기획자도 함께 읽고 이해해야 하는 문서입니다 따라서 이렇게 개발 용어처럼 작성하는 것은 추천하지 않습니다 이런 표현은 설계 구현 문서에 사용하는 것이 적절합니다 여기서는 편의를 위해 이렇게 작성한 것 뿐입니다 물론 정해진 규칙은 없습니다 지금 설명한 내용도 절대적인 기준이 아닙니다 프로젝트의 상황에 따라 가장 적절한 판단을 내리는 것이 중요합니다 여기서 설명하는 것은 하나의 사례일 뿐입니다 우리는 이제 절차가 복잡해 보이는 두 유스케이스인 상영시간 생성과 티켓 구매에 대해 분석을 진행해야 하는데요 어떤 유스케이스부터 분석하는 것이 좋을까요? 저는 보통 데이터를 생성하는 유스케이스부터 시작하는 편입니다 프로그램은 동작 가능한 상태를 유지하는 것이 좋습니다 그런데 데이터 생성 기능 없이 조회 기능부터 구현하면 테스트나 실행이 어려운 경우가 많습니다 이 경우에는 상영시간을 먼저 생성해야 티켓을 구매할 수 있습니다 따라서 CreateShowtimes 유스케이스부터 분석을 해보도록 하겠습니다

### 2. [02:00 - 04:00]

![frame 2](output/frames/frame_002.jpg)

우리는 도메인 전문가에게 상영시간을 생성하려면 어떤 절차가 필요한지 물어본 뒤, 이렇게 유스케이스 명세서를 작성할 수 있습니다 상영시간 생성을 시작하는 조건인 트리거를 정의하고요 그에 따른 기본 흐름을 정리했습니다 그리고 대안 흐름, 사후 조건 등을 통해 Movie Booking System이 수행해야 할 작업들을 가늠할 수 있습니다 이 중에서 목표, 선행 조건, 사후 조건 같은 항목들은 다소 형식적으로 느껴질지도 모르겠습니다 이 정보들은 나중에 테스트 케이스를 작성할 때 그대로 활용됩니다 분석 단계에서 미리 정리해 두면 테스트 작성 시 수고를 덜 수 있습니다 이 문서는 평문 형식이기 때문에 누구나 쉽게 읽고 정리할 수 있다는 장점이 있습니다 다만 아쉬운 점은 기본 흐름이나 대안 흐름의 절차가 쉽게 파악되지 않는다는 것입니다 유스케이스 명세서의 기본 흐름을 시퀀스 다이어그램으로 그려봤습니다 이 시퀀스 다이어그램은 액터, 트리거, 기본 흐름의 내용을 담고 있는데요 Admin과 Movie Booking System이 주고받는 메시지가 한눈에 들어오기 때문에 기본 흐름을 쉽게 파악할 수 있습니다 이번에는 alt 연산자를 사용해서 대한 흐름을 그려봤습니다 alt 연산자를 사용한 흐름 표현 자체는 나쁘지 않아 보입니다 다만, 기본 흐름 5단계로 이동하라는 부분은 직관적으로 이해하기 어려운 면이 있습니다 기본 흐름 5단계로 이동하라는 것은 상영 시간 입력을 반복하라는 의미입니다 따라서 왼쪽 시퀀스 다이어그램처럼 loop 연산자를 사용해서 표현할 수 있습니다 loop 연산자를 사용하니까

### 3. [04:00 - 06:00]

![frame 3](output/frames/frame_003.jpg)

실패 시 동작이 반복된다는 흐름이 더 명확하게 드러납니다 다만, loop와 alt 프레임이 중첩되면서 분석 단계 문서임에도 다소 복잡하게 보입니다 분석의 목적은 요구 사항이 무엇인지 파악하는 데 있습니다 이 단계에서 alt 같은 조건 분기 연산자를 사용하면 분석 작업이 복잡해지고 구현 중심으로 흐를 우려가 있습니다 지금은 예제가 간단해서 큰 문제가 되지 않지만, 경우에 따라 대안 흐름이 여러 개 존재할 수 있습니다 이 모든 흐름을 하나의 다이어그램에 표현하려 하면 분석 문서가 지나치게 복잡해질 것입니다 이런 경우, 대안 흐름을 기본 흐름과는 다른, 별도의 시퀀스 다이어그램으로 그리는 것도 한 방법입니다 설계는 구현에 필요한 만큼만 하면 충분합니다 여기서 필요한 만큼이라는 표현이 다소 모호한데요 이것은 팀의 상황에 따라 달라집니다 예를 들어, 개발자의 실력이 높고, 팀 간 커뮤니케이션이 원활하다면, 간단한 설계만으로도 충분할 수 있습니다 반면에 설계자와 구현자가 긴밀한 소통이 어려운 환경이라면 좀 더 꼼꼼한 설계가 필요할 것입니다 여기서는 대안 흐름의 내용이 간단함에도 불구하고, 설명을 위해 시퀀스 다이어그램을 작성했습니다 실제 프로젝트라면 아마도 작성하지 않았을 것 같습니다 만약 화면 기획자나 디자이너라면, 시퀀스 다이어그램을 여기서 더 확장하지 않아도 괜찮을 것 같습니다 그러나 우리는 백엔드를 대상으로 하고 있으니까, 시퀀스 다이어그램을 확장해서 REST API 설계를 해봅시다 보시는 것과 같이 REST API를 설계했는데요

### 4. [06:00 - 08:00]

![frame 4](output/frames/frame_004.jpg)

영화 목록은 /movies로 극장 목록은 /theaters로 요청합니다 이것은 경로를 짧게 가져가는 전형적인 Shallow Routing 설계 방식입니다 URL 구조를 단순하게 유지함으로써, 리소스에 대한 접근과 관리가 용이해지는 장점이 있습니다 일반적으로는 이와 같은 방식으로 설계하는 것이 좋은 사례로 여겨집니다 그러나 Shallow Routing 방식은 몇 가지 단점이 있습니다 지금은 프로젝트 초기 단계이기 때문에 API가 간단하지만, 프로젝트가 진행되고 요구사항이 구체화되면, API가 자주 변경될 수 있습니다 예를 들면, 이렇게 말이죠 영화 목록을 개봉일로 정렬해야 해요 /movies의 정렬 옵션 추가할게요 아, 화면이 바뀌었어요 상영 시간 정보도 같이 받아야 해요 showtime-summary 옵션 추가할게요 아, 어쩌죠? 이번엔 상영 시간 정보가 좀 바뀌어야 한대요 showtime-summary2로 변경합시다 그냥 기존 옵션 사용하면 안 돼요? 안 됩니다 다른 곳에서 showtime-summary 사용하고 있어요 고치면 다른 쪽이 영향받아요 헐 이러다가 showtime-summary3도 생기겠네요? showtime-summary13까지 안 가면 다행이죠 참으로 체할 것 같은 대화입니다 여러분도 한 잔 하시기 바랍니다 왜 이런 문제가 생겼을까요? 그것은 하나의 API 에 너무 많은 기능을 담으려 했기 때문입니다 Shallow Routing 방식에서는 복합적인 데이터 요청을 하나의 API 에서 처리하는 것이 아니라 프론트엔드가 백엔드를 여러 번 호출해서 동작을 구현해야 하는 것이죠 그러나 화면 구현에 집중해야 하는 프론트엔드 입장에서는

### 5. [08:00 - 10:00]

![frame 5](output/frames/frame_005.jpg)

백엔드로부터 여러 데이터를 받아 가공하고 조합하는 작업이 큰 부담이 됩니다 프런트가 프론트가 아니게 되어 버리는 것이죠 이런 작업은 백엔드에서 하는 게 합리적인 것 같습니다 이 문제를 해결하기 위해서 REST API에 namespace를 적용했습니다 오른쪽에 정리된 API 를 보면, 모든 경로 앞에 /showtime-creation이 추가된 것을 확인할 수 있습니다 /showtime-creation/movies는 상영시간 생성을 위한 영화 목록을 의미하고, /showtime-creation/theaters는 상영시간 생성을 위한 극장 목록을 의미합니다 이렇게 API 를 정의하면 상영시간 생성이라는 목적이 명확히 드러납니다 해당 요구사항이 크게 변경되지 않는 한, API 를 변경할 필요도 줄어들게 됩니다 이런 설계 방식을 누군가는 컨텍스트 컨트롤러 패턴이라고 부른다고 합니다 저는 패턴이 좀 거창하게 느껴져서 단순히 namespace라고 부르도록 하겠습니다 소프트웨어 개발은 지속적인 변화를 전제로 하기 때문에 유연성을 중요하게 여깁니다 그러나 API에서 유연성을 높인다는 것은, 그만큼 사용과 구현이 모두 어려워진다는 의미이기도 합니다 다시 말해서, API 의 옵션이 복잡해질수록, 이를 처리해야 하는 백엔드의 구현 부담이 커집니다 API 를 사용하는 프론트엔드 역시 스펙을 이해하고 구현하는 데 더 많은 노력을 들여야 합니다 만약 불특정 다수를 대상으로 하는 API 라면, 유연성을 높여서 구체적인 구현을 사용자에게 맡기는 것이, 합리적일 수 있습니다 하지만 지금은 상영시간 생성이라는 명확한 요청임을 알고 있는 상황입니다 그렇다면 API 역시 이 목적이 잘 드러나도록 정의하는 것이

### 6. [10:00 - 12:00]

![frame 6](output/frames/frame_006.jpg)

더 합리적인 접근일 것입니다 우리는 지금까지 Top-down 방식으로 분석과 설계를 진행했습니다 그 과정에서 자연스럽게 /showtime-creation과 같은 namespace를 떠올릴 수 있었습니다 왜냐하면, 상영시간 생성이라는 문제 중심으로 사고했기 때문이죠 그렇다면, Bottom-up 방식 즉, 구현을 우선하는 접근을 한다면 어떻게 될까요? 먼저 파악된 도메인 객체들을 중심으로 DB 스키마를 정의합니다 그리고 그 스키마를 그대로 노출하는 CRUD API 를 작성합니다 그리고 새로운 요구사항이 생기면, 기존 구현 범위 안에서 해결책을 찾으려고 합니다 다시 말해서, 기존에 존재하는 API 를 보면서, 아, 요구사항을 어떻게 구현하지? 라고 고민한다는 뜻이죠 이 과정에서 상영 시간 생성이라는 상위 개념은 약해집니다 그리고 영화, 극장, 티켓같이 이미 구현된 API 를 기반으로 발전하게 됩니다 현재 구현을 기반으로 상위 개념과 요구 사항을 만족시키려고 하기 때문에 문제의 본질과 구현이 점점 멀어지게 되는 것이죠 본질 기반 해석에서 말하는 것과 완전히 반대가 됩니다 지금까지 설명이 다소 추상적이라서 쉽게 납득하기 어려우실지도 모르겠습니다 그런 분들은 이 프로젝트의 전체 흐름을 여러 번 되새기면서 Top-down 사고 과정을 따라가 보시면 좋을 것 같습니다 지난 시간에 정의한 최우선 요구 사항을 다시 확인해 보면, 극장은 최소 4,000개 이상이라는 내용이 포함되어 있습니다

### 7. [12:00 - 14:00]

![frame 7](output/frames/frame_007.jpg)

극장이 이렇게 많으면, 극장ID를 배열로 받는 모든 API 에서 문제가 될 수 있습니다 여기서는 상영시간 조회 API 가 그런데요 극장ID 목록을 쿼리 파라미터로 전달하도록 되어 있습니다 이런 경우에는 POST 방식으로 API 를 정의하도록 하겠습니다 POST는 원래 리소스를 생성한다는 의미이지만, 여기서는 실행한다는 의미로 사용했습니다 그래서 POST /showtimes/search 상영 시간 검색을 실행한다는 의미로 해석하면 됩니다 지금까지 설명한 설계 지침을 반영해서 시퀀스 다이어그램을 다시 그렸습니다 오른쪽에는 변경된 REST API 를 정리했습니다 지금까지 REST API를 설계했으니까 이제 REST API 와 연결되는 서비스를 설계해 봅시다 서비스 설계를 시작하기 전에 먼저 프로젝트와 개발팀의 상황을 고려해서 최상위 아키텍처를 결정해야 합니다 여기서는 마이크로서비스 아키텍처를 선택했습니다 MSA 의 가장 큰 특징은 작은 단위의 서비스들이 서로 협력해서 하나의 전체 서비스를 구성한다는 점입니다 이때 중요한 원칙 중 하나는 서비스 간의 데이터베이스를 공유하지 않는다는 것입니다 이것은 서비스 간 결합도를 낮추고 독립적인 운영을 가능하게 합니다 "DB를 공유하지 않는다" 마이크로서비스의 DB 를 공유하지 않는 특징은 객체 지향 프로그래밍에 클래스와 유사한 면이 있습니다 마이크로서비스가 자신의 DB와 API 를 가지고 독립적으로 동작한다는 점은 클래스의 프로퍼티와 메소드를 떠올리게 합니다

### 8. [14:00 - 16:00]

![frame 8](output/frames/frame_008.jpg)

결국 MSA는 개념적으로 객체 지향 방식과 유사한 부분이 있는 것이죠 그러면 이제, 시퀀스 다이어그램을 확장해서 MSA 에 맞게 설계를 해봅시다 시퀀스 다이어그램을 보면 REST API 에 맞춰서 각각 MoviesService, TheatersService, ShowtimesService를 호출하고 있습니다 이 시퀀스 다이어그램에서 그나마 복잡해 보이는 부분은 ShowtimesService의 createShowtimes즈 함수입니다 이 함수는 상영 시간 생성 요청을 검증하기 위해서 MoviesService의 moviesExist 함수와 TheatersService의 theatersExist 함수를 호출하고 있습니다 객체 지향 프로그래밍에서는 순환 참조를 피하라고 합니다 그 이유는 메모리 관리 같은 기술적인 문제도 있지만, 그보다 더 중요한 이유는 두 객체가 강하게 결합된다는 점입니다 예를 들어, 클래스A 와 클래스B 가 서로를 참조한다고 가정해봅시다 이 경우 A에 변경이 생기면 B가 영향을 받고 B를 수정하면 다시 A에 영향이 갑니다 이건 결국 A와 B가 하나의 객체처럼 얽히는 것과 마찬가지인 상태가 되는 것이죠 이 문제를 해결하기 위해서 인터페이스나 의존 주입 같은 다양한 방법이 있지만, 가장 근본적인 해결책은 애초에 순환 참조 관계를 만들지 않는 것입니다 자, 다시 이 시퀀스 다이어그램을 보면 상영 시간 생성 요청을 처리하기 위해서 ShowtimesService가 MoviesService를 참조하고 있습니다 여기에 특정 시간대에 상영 중인 영화 목록을 조회하는 기능이

### 9. [16:00 - 18:00]

![frame 9](output/frames/frame_009.jpg)

MoviesService에 추가된다고 가정해봅시다 이 기능을 구현하려면 MoviesService가 ShowtimesService를 참조해야 합니다 이렇게 되면 두 서비스가 서로를 참조하는 순환 참조 문제가 생기는 것이죠 결국 한 서비스가 다른 서비스를 참조할 수 있다면 언젠간 상호 참조 문제가 발생할 수 있는 것입니다 앞에서 MSA 를 설명할 때 마이크로서비스가 협력해서 전체 서비스를 제공하는 것이라고 정의했는데요 여기서는 서비스가 다른 서비스를 참조하면 안 된다고 얘기하고 있습니다 도대체 어쩌라는 걸까요? 서비스 간 협력과 순환 참조 금지라는 상충하는 요구 사항을 해결하기 위해서 마이크로서비스 아키텍처에 서비스 레이어 개념을 적용합니다 이것은 하나의 마이크로서비스는, 하위 레이어에 존재하는 마이크로서비스만 참조할 수 있다는 규칙입니다 동일 계층에 있는 다른 마이크로서비스도 참조할 수 없습니다 이 프로젝트에서는 각 마이크로서비스를 Application, Core, Infrastructure의 세 레이어로 분리했는데요 이것은 객체를 Application, Domain, Infrastructure로 나누는 전통적인 Layered Architecture와 유사합니다 앞으로 이 아키텍처를 Service-oriented Layered Architecture라고 줄여서 SoLA라고 부르겠습니다 혹시 기존에 유사한 개념에 대해 알려진 명칭이 있다면, 댓글로 공유해 주시면 감사하겠습니다 SoLA와 Layered Architecture를 비교하는 화면인데요 둘의 구조가 유사한 것을 알 수 있습니다

### 10. [18:00 - 20:00]

![frame 10](output/frames/frame_010.jpg)

차이점이라면, Layered Architecture가 객체를 계층화하는 반면에 SoLA는 서비스를 계층화한다는 정도죠 그러면 SoLA와 REST API 가 어떻게 대응되는지 살펴봅시다 앞서 정의한 REST API 의 namespace는 SoLA에서 Application 레이어에 해당합니다 즉, /showtime-creation과 같은 특정 기능 단위의 API 들은 대부분 Application 서비스와 연결되는 것입니다 반면에 namespace가 없는 일반적인 엔티티 컬렉션에 대한 요청은 SoLA에 Core 레이어의 서비스를 호출하게 됩니다 Application 서비스와 Core 서비스의 API 를 비교해보면, Application 서비스는 특정한 유스케이스를 직접 지원하기 때문에, API 가 단순하고 직관적인 형태가 됩니다 반면에 Core 서비스는 다양한 Application 서비스에서 재사용되므로 보다 범용적이고 유연한 API 가 됩니다 SoLA의 Infrastructure 레이어는 무슨 일을 할까요? Infrastructure 레이어의 서비스는 외부 서비스나 시스템을 지원합니다 그래서 지난 시간에 문맥 다이어그램에서 정의했던 외부 의존 관계는 대부분 Infrastructure 레이어에 위치한 서비스와 대응됩니다 화면에서 Payment Gateway가 Infrastructure 레이어의 PaymentsService와 연결되어 있는 것을 볼 수 있습니다 그리고 레거시 시스템을 호출하는 일도 Infrastructure 레이어의 서비스에서 해야겠죠 여기에 구체적으로 언급하지는 않았지만, AWS 나 GCP 같은 클라우드 서비스와의 연결 역시 Infrastructure 레이어에서 처리되는 대표적인 예입니다 그러면 지금까지 설명한 SoLA를 적용하여

### 11. [20:00 - 22:00]

![frame 11](output/frames/frame_011.jpg)

서비스를 다시 설계해 봅시다 /showtime-creation namespace에 맞춰 ShowtimeCreationService라는 Application 서비스를 새로 만들었습니다 REST API 의 namespace가 응용 서비스와 자연스럽게 대응되기 때문에 구조를 파악하기가 한결 쉬워지는 장점이 생깁니다 그리고 ShowtimeCreationService의 createShowtimes 함수가 MoviesService와 TheatersService를 호출하도록 구성되면서 ShowtimesService가 다른 서비스를 참조하지 않게 됐습니다 이렇게 Application 서비스를 구성하면 서비스 간 순환 참조를 원천적으로 차단할 수 있는 것입니다 여기서 살짝 마음에 걸리는 부분은 ShowtimeCreationService에 searchMovies 를 호출하면 실제로는 MoviesService에 searchMovies 를 그대로 다시 호출하고 있다는 것입니다 이것은 단순 위임이라는 안티 패턴으로 보입니다 이걸 그냥 둬도 괜찮은 걸까요? 화면을 보면 직접 호출과 단순 위임 호출이 보이는데요 여기서는 REST API 가 MoviesService를 직접 호출하는 게 더 합리적으로 보입니다 그렇다면 무엇이 옳은 걸까요? 여러분은 어떻게 생각하십니까? 이런 상황이 발생한 이유는 단순 위임은 금지한다는 규칙과 상영시간 생성은 ShowtimeCreationService에서 처리한다는 규칙이 충돌하기 때문입니다 이런 경우에는 두 규칙의 우선 순위를 따져봐야 하는데요 여기서 더 중요한 규칙은 상영 시간 생성은 ShowtimeCreationService에서 처리한다 입니다 따라서 이 구조는 안티 패턴이 아닙니다

### 12. [22:00 - 24:00]

![frame 12](output/frames/frame_012.jpg)

그리고 지금은 프로젝트 초기 단계이기 때문에 단순 위임이지만, 기능이 점점 구체화되면 ShowtimeCreationService의 searchMovies 함수는 더 많은 로직이나 조건 처리를 포함할 수 있습니다 이런 확장 가능성을 고려해보면 현재의 구조를 안티 패턴으로 볼 수 없습니다 그럼에도 불구하고 이 구조가 어딘가 찜찜하게 느껴진다면 그건 아마도 구현을 우선시하는 Bottom-up 사고방식에 익숙한 영향일 수 있습니다 그러나 현재의 구현 관점이 아니라 설계 원칙에 따라 판단하는 것이 옳은 방향일 것입니다 결론입니다 지금까지 수행해 온 분석 설계 과정을 되짚어 보도록 합시다 우리는 도메인 전문가와 대화를 통해 최우선 요구사항을 정리했습니다 그리고 다시 도메인 전문가와 대화를 통해 사용자로 고객과 관리자가 있음을 알았습니다 그리고 고객과 관리자가 각각 어떤 일을 하는지도 정리할 수 있었습니다 이렇게 정리한 많은 유스케이스 중에서 이번 시간에는 상영시간 생성 유스케이스에 대해서 명세서를 작성했습니다 그리고 상영시간 생성 유스케이스의 기본 흐름을 시퀀스 다이어그램으로 그렸습니다 그리고 시퀀스 다이어그램을 확장해서 REST API 를 설계했죠 그리고 시퀀스 다이어그램을 더 확장해서 REST API 에 대응하는 서비스 설계를 했습니다 지금까지의 분석 설계 과정을 거치면서 유스케이스, REST API, 마이크로서비스 구조가 자연스럽게 일치하게 되었습니다 자연스럽게 일치됐다는 것은

### 13. [24:00 - 26:00]

![frame 13](output/frames/frame_013.jpg)

이들을 일치시키려고 의도한 것이 아니라는 뜻입니다 Top-down 방식으로 설계를 진행하다 보니 결과적으로 자연스럽게 일치하게 된 것입니다 개발은 이렇게 물 흐르듯이 자연스러워야 합니다 마지막으로 강조하고 싶은 점은 우리가 참고할 모범 사례는 존재하지만 절대적인 정답은 없다는 사실입니다 흔히 좋다고 알려진 기술이나 패턴은 특정 맥락에서 효과적이었던 경험적 증거일 뿐 만능 열쇠가 아닙니다 TDD 나 DDD 도 모든 프로젝트에 똑같이 유용하지는 않습니다 프로젝트와 개발팀의 환경이 모두 다르기 때문입니다 따라서 현재 상황을 면밀히 분석한 뒤, 가장 합리적인 방식을 스스로 판단해야 합니다 뿐만 아니라 기존 모범 사례를 응용해서 현실에 적용할 수 있어야 합니다 왜냐하면 프로젝트와 개발팀의 환경이 모두 다르기 때문입니다 그리고 이렇게 응용해서 적용하려면 OOP 에 대한 깊은 이해와 문제의 본질을 꿰뚫어보는 통찰력이 필수적입니다 오늘 소개한 SoLA와 REST API 의 namespace도 이런 과정을 거치면서 이번 프로젝트에서 고안한 것입니다 이렇게 응용을 강조하는 이유는 그것이 Top-down 사고 과정이기 때문입니다 문제를 통찰하고 OOP적 사고를 통해서 해결책을 생각해야 합리적인 해결 방법을 선택할 수 있습니다 그런데 많은 경우 기존 모범 사례에 현재 상황을 맞추려고 합니다 마치 구현에 요구 사항을 끼워 맞추듯 말이죠 어쩌면 우리는 생각보다 Bottom-up 사고방식에 더 익숙해져 있는지도 모릅니다

### 14. [26:00 - 26:13]

![frame 14](output/frames/frame_014.jpg)

다음 시간에는 상영시간 생성 기능에 부족했던 설계를 보완하고 그에 대한 테스트 코드를 작성해 보겠습니다 지금까지 수고하셨습니다
